spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB
---
spring:
  profiles: dev
logging:
  level:
    org:
      springframework:
        web: DEBUG
server:
  port: 0
---
spring:
  profiles: prod
server:
  port: 8080    